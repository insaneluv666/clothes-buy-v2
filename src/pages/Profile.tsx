import styled from "styled-components";

const Profile = () => {
  return (
    <ProfileWrapper>
    <span>Profile</span>
    </ProfileWrapper> 
  );
}

const ProfileWrapper = styled.div`

`
 
export default Profile;