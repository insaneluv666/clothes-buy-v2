import styled from "styled-components";

const Products = () => {
  return (
    <ProductsWrapper>
    <span>Products</span>
    </ProductsWrapper> 
  );
}

const ProductsWrapper = styled.div`

`
 
export default Products;