import styled from "styled-components";

const Product = () => {
  return (
    <ProductWrapper>
    <span>Product</span>
    </ProductWrapper> 
  );
}
 
const ProductWrapper = styled.div`

`

export default Product;